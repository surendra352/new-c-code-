#include<stdio.h>

int main(){
    int a=4;
    int b=6;
    int c=8;
    printf("the number of value in %d\n",a*b/c+7);
    printf("the value is %d",b*3/2*c + 7*a);
    // the value of is b*3/2*c+28
    // the value of is 18/2*c+28
    // the value of is 9*c+28
    return 0;
}